[project]
name = "texttomorse"
version = "0.0.1"
dependencies = [
    "tones",
    "pygame"
]

[tool.setuptools.packages.find]
# All the following settings are optional:
where = ["src"]  # ["."] by default
include = ["texttomorse*"]  # ["*"] by default

[project.scripts]
ttm = "texttomorse:main"